/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['images.unsplash.com',"assets.aceternity.com","img.freepik.com","unsplash.com"],
  },
};

export default nextConfig;
